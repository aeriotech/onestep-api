{
  "routes": [
    {
      "method": "GET",
      "path": "/squads",
      "handler": "squad.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/squads/count",
      "handler": "squad.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/squads/:id",
      "handler": "squad.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/squads",
      "handler": "squad.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/squads/:id",
      "handler": "squad.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/squads/:id",
      "handler": "squad.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
